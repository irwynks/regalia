import { Outlet } from "react-router-dom";
import { Container, Nav } from 'react-bootstrap';
import './footer.scss';

export const Footer = () => {
    return (
        <div className="footer">
            CREATED BY IRWYNKS
        </div>
    );
};
